rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /animations/{docId} {
      allow read: if true;
      allow create, update, delete: if request.auth != null;
    }
  }
}
